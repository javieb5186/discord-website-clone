// A simple Custom MUI button

"use client";
import { Button } from "@mui/material";
import { styled } from "@mui/material/styles";

export const SimpleButton = styled(Button)({
  textTransform: "none",
  borderRadius: "1rem",
  height: "2rem",
});
